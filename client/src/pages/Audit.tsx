import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { ClipboardList, ShieldAlert } from "lucide-react";
export default function AuditPage() { const logs = trpc.audit.recent.useQuery({ limit: 50 }); return <div className="space-y-7"><div><p className="eyebrow">GOVERNANCE / AUDIT</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">Journal d’audit</h1><p className="mt-2 text-sm text-slate-500">Les connexions et modifications sensibles sont tracées pour faciliter l’investigation.</p></div><Card className="border-0 shadow-sm"><CardHeader className="border-b border-slate-100"><CardTitle className="flex items-center gap-2 text-base"><ClipboardList className="h-4 w-4 text-emerald-600" />Événements récents <span className="status status-implemented ml-auto">IMPLEMENTED</span></CardTitle></CardHeader><CardContent className="p-0"><div className="divide-y divide-slate-100">{logs.isLoading ? <p className="p-8 text-sm text-slate-400">Chargement depuis l’API…</p> : logs.data?.map(log => <div key={log.id} className="flex items-start gap-4 px-6 py-4"><div className="mt-1 grid h-8 w-8 place-items-center rounded-lg bg-slate-100"><ShieldAlert className="h-4 w-4 text-slate-500" /></div><div className="min-w-0 flex-1"><p className="text-sm font-medium text-slate-800">{log.action}</p><p className="mt-1 text-xs text-slate-400">Cible : {log.targetType || "—"} {log.targetId || ""} · Acteur #{log.actorUserId || "système"}</p></div><time className="text-xs text-slate-400">{new Date(log.createdAt).toLocaleString("fr-FR")}</time></div>)}{!logs.isLoading && !logs.data?.length && <p className="p-10 text-center text-sm text-slate-400">Aucun événement d’audit n’est encore disponible.</p>}</div></CardContent></Card></div>; }
