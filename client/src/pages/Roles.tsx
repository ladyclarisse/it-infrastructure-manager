import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { Check, LockKeyhole } from "lucide-react";
const capabilityRows = [["Consulter les utilisateurs", ["admin", "it_manager"]], ["Modifier un rôle", ["admin", "it_manager"]], ["Désactiver un compte", ["admin", "it_manager"]], ["Consulter l’audit", ["admin"]], ["Administrer l’infrastructure", ["admin", "systems_network_admin"]], ["Superviser le monitoring", ["admin", "systems_network_admin", "it_manager"]] ] as const;
const roles = [["admin", "Administrateur"], ["systems_network_admin", "Administrateur systèmes/réseaux"], ["technician", "Technicien"], ["it_manager", "Responsable informatique"], ["user", "Utilisateur"]] as const;
export default function RolesPage() { const data = trpc.users.roles.useQuery(); const displayedRoles = data.data?.map(role => ({ slug: role.slug, name: "name" in role ? role.name : role.label, description: role.description })) || roles.map(([slug, name]) => ({ slug, name, description: "Catalogue local du socle RBAC." })); return <div className="space-y-7"><div><p className="eyebrow">POLICY / RBAC</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">Rôles & permissions</h1><p className="mt-2 text-sm text-slate-500">La politique d’accès est évaluée sur le serveur pour chaque procédure.</p></div><Card className="border-0 shadow-sm"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><LockKeyhole className="h-4 w-4 text-violet-600" />Matrice de contrôle</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><th className="px-3 py-3 font-medium">Capacité</th>{roles.map(([, label]) => <th key={label} className="px-3 py-3 text-center font-medium">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{capabilityRows.map(([label, allowed]) => <tr key={label}><td className="px-3 py-4 font-medium text-slate-700">{label}</td>{roles.map(([slug]) => <td key={slug} className="px-3 py-4 text-center">{allowed.includes(slug as never) ? <Check className="mx-auto h-4 w-4 text-emerald-500" /> : <span className="text-slate-200">—</span>}</td>)}</tr>)}</tbody></table></CardContent></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{displayedRoles.map(role => <Card key={role.slug} className="border-0 shadow-sm"><CardContent className="p-5"><span className="status status-implemented">IMPLEMENTED</span><h2 className="mt-4 text-sm font-semibold text-slate-900">{role.name}</h2><p className="mt-2 text-xs leading-5 text-slate-500">{role.description}</p></CardContent></Card>)}</div></div>; }
